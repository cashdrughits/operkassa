server {
    listen 80;
    server_name operkassa.online;

    # Redirect HTTP → HTTPS (раскомментируйте после настройки SSL)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://127.0.0.1:5000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }

    # Защита /admin — ограничить по IP (опционально)
    # location /admin {
    #     allow 1.2.3.4;   # ваш IP
    #     deny all;
    #     proxy_pass http://127.0.0.1:5000;
    # }
}

# HTTPS (раскомментируйте после certbot)
# server {
#     listen 443 ssl http2;
#     server_name operkassa.online;
#     ssl_certificate     /etc/letsencrypt/live/operkassa.online/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/operkassa.online/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#
#     location / {
#         proxy_pass http://127.0.0.1:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }